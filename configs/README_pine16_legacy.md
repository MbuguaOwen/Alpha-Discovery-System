# Legacy DFD05 Configs

`configs/dfd05_*.yaml` are legacy research configs.

They are **not** exact Pine-exported truth configs and must be treated as:

- `UNVERIFIED_PYTHON_APPROXIMATION` unless explicitly reconciled against TradingView exports.

Use `configs/pine16_exact_*.yaml` for Pine16 exact workflow.
